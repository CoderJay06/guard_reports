development:
  adapter: postgresql
  database: development
  host: localhost
test:
    adapter: postgresql
    database: test
    host: localhost
production:
  adapter: postgresql
  database: production
  host: localhost